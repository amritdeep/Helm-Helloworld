repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
  - name: helloworld
    namespace: default
    chart: ./helloword
    values:
      - values.yaml
    installed: true

  - name: helloworld2
    namespace: default
    chart: ./helloword
    installed: true