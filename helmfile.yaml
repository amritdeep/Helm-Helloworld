repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
  - name: helloworld
    namespace: default
    chart: ./helloword
    values:
      - values.yaml
    installed: true